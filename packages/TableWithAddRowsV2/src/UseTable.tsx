import { SetupContext } from "vue"
import { ElTable } from "element-plus"

const emitNames = ['change', 'expand-change', 'blur', 'focus', 'visible-change', 'remove-tag']

export function useTable<T>(props: any, context?: SetupContext<{}>,) {

  return {
   
  }
}
